go inside the folder "todo-list"

run npm install

run npm start

go to localhost:3000 in browser

finished edit and delete buttons, all seems ok. The only problem is the tasks get sorted depending on id, which is decided when they are created, instead of when they are moved in their category.