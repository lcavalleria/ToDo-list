go inside the folder "todo-list"

run npm install

run npm start

go to localhost:3000 in browser

for now it's only displaying some hardcoded tasks, cannot add/edit tasks, buttons are empty and css is very basic.